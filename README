fastboot use:
1 not connect usb
2 connect uart
3 power on & input in uart
4 in uart commandline input follow
    4.1 usbgadget.vendor=0x18d1
    4.2 usbgadget.product=0x0003
    4.3 ls /dev to check partition
    4.4 usbgadget -A /dev/nand0.boot(boot),/dev/nand0.system(system)
5 connect usb
6 fastboot flash boot boot.img

PS:now only can download boot,because boot not use filesystem.
    system use yaffs2,but now barebox do not support yaffs filesystem and like yaffs2format command

